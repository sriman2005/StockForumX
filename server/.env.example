# Server Configuration
NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:5173

# Logging (debug, info, http, warn, error)
LOG_LEVEL=debug

# Database
MONGODB_URI=mongodb://localhost:27017/stockforumx

# Security
# Must be at least 32 characters long
JWT_SECRET=your_super_secret_jwt_key_please_change_this

# Email Configuration (Optional - falls back to Ethereal)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
FROM_NAME=StockForumX
FROM_EMAIL=noreply@stockforumx.com
