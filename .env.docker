# Server Configuration
PORT=5000
NODE_ENV=production

# Database
# Use container name 'mongodb' as host
MONGODB_URI=mongodb://mongodb:27017/stockforumx

# Security
JWT_SECRET=replace_this_with_a_secure_random_string_in_production

# Client URL
# The external URL where the app will be accessed
CLIENT_URL=http://localhost

# Email (Optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=StockForumX <noreply@stockforumx.com>
